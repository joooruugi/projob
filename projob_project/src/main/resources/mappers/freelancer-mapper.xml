<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="Freelancer">
	<insert id="insertFreelancer" parameterType="Freelancer">
		insert into fruser(fr_crn, fr_id, fr_password, fr_name, fr_nick, fr_phone, fr_email, fr_address, fr_birth)
		values (#{frCrn}, #{frId}, #{frPassword}, #{frName}, #{frNick}, #{frPhone}, #{frEmail}, #{frAddress}, #{frBirth})
	</insert>
</mapper>
