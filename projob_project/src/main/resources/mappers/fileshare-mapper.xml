<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="Fileshare">

	<select id="selectProject" resultType="Project">
		select pro_no, pro_title, pro_content from project left join p_member using(pro_no) where us_id=#{us_id}
	</select>
	
	<select id="selectFileshareList" parameterType="String" resultType="map">
	<![CDATA[
		select sh_no, pro_no, pro_title, sh_id, sh_title, sh_content, to_char(sh_wdate, 'yyyy/mm/dd HH24:MI') sh_wdate from sharee left join project using(pro_no) where pro_no=#{pro_no} and sh_useyn='Y' order by sh_wdate
	]]>
	</select>
</mapper>
